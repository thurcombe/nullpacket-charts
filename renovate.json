{
  "extends": [
    "config:recommended"
  ],
  "bumpVersions": [
    {
      "filePatterns": [
        "charts/jellyfin/Chart.{yaml,yml}"
      ],
      "matchStrings": [
        "version:\\s(?<version>[^\\s]+)"
      ],
      "bumpType": "{{#if isPatch}}patch{{else}}minor{{/if}}"
    }
  ]
}
